# GitHub Pages 发布流程

当前最新版网站目录：

`/Users/hawksky/Desktop/modern phi/modern philosophy website`

当前 GitHub Pages 仓库：

`https://github.com/hawks0261-sudo/modern-philosophy-website.git`

## 先生成、检查，再发布

2026-09-05 审查修复在独立分支 `codex/site-audit-phase1-20260905` 与工作树中完成：

`/Users/hawksky/Desktop/modern phi/worktrees/site-audit-phase1`

原始 `main` 工作目录仍可能包含用户尚未整理的修改。不要直接切换分支、覆盖文件或把原目录的全部变更加入提交。先在修复工作树检查状态与差异：

```sh
git status --short
git diff --stat
python3 scripts/build_site.py --all
python3 scripts/test_build.py
python3 scripts/build_site.py --check
python3 scripts/check_site.py
git diff --check
```

确认后在浏览器查看首页、活动归档与详情、图书目录与独立详情、成员页、中英文入口和期刊投稿入口；至少检查桌面、手机、键盘与关闭 JavaScript 后的导航。维护入口与生成边界见 `scripts/README.md`。

只暂存本次已检查的文件。对已跟踪文件可使用 `git add -p`，对新增文件使用逐个路径的 `git add -- 路径`；核对以下清单后再提交：

```sh
git diff --cached --name-status
git diff --cached --stat
```

经授权正式上线时，再把经过审阅的修复分支合并到发布分支并推送；实际目标以当前远程与 GitHub Pages 设置为准。本地生成、提交或推送成功都不代表部署完成。不得把输出截图、验收报告、历史草稿、临时文件或用户无关修改一起提交。

GitHub Actions 的 `Static site checks` 只执行生成一致性、内容传播与静态完整性检查，权限为仓库读取，不部署站点。正式上线后，检查部署任务状态与实际线上页面，再确认旧入口 404 和 PDF 直链正常。

## 发布目录边界与维护归档

此站点目前直接发布 `main` 分支根目录，且使用 `.nojekyll`；根目录中提交的文件都应视作可公开访问。不要把历史 HTML、临时渲染器、截图或维护备份放在网站目录中，也不要依靠 `robots.txt` 隐藏它们。

2026-09-05 第一批修复从待发布源移除了以下两个无正式页面引用的文件：

- `pdf-render.html`：旧 PDF.js 2.16.105 渲染辅助页，不能重新执行或发布。公开 PDF 文件及其直接下载链接继续保留。
- `uploads/index.html`：旧版整站草稿。`uploads` 中正式页面仍使用的活动图片、封面和 PDF 继续保留。

原件已逐字节保存到网站仓库之外的 `output/site-fixes-20260905/archived-maintenance/`，归档以 `.html.txt` 命名并设为只读。该目录的 `manifest.json`、`SHA256SUMS` 和 `README.md` 记录原路径、校验值和恢复方法；Git 历史也保留原件。恢复只用于维护查阅，不能把旧页面直接放回发布源。

`.gitignore` 已列出这两个旧入口及仓库内的 `archived-maintenance/`，防止常规误加入；它不能阻止 `git add -f`，也不能保护已跟踪的文件。提交前核对实际文件列表：

```bash
git status --short
git diff --stat
test ! -e pdf-render.html && test ! -e uploads/index.html
git diff --cached --name-status
```

本地删除不代表线上删除。经审核推送并等待部署完成后，重新检查首页、活动详情、图书页、成员页和 PDF 直链；另外确认 `pdf-render.html` 与 `uploads/index.html` 均返回 404，不能仅凭推送成功宣布清理完成。

## 自动版：以后正式上线时直接发给 Codex 的命令

下面这些不是终端命令，而是你以后可以直接发给我的指令模板。到时你准备好了，直接复制一句给我即可。

### 1. 正式上线前的最终检查

```txt
请按正式上线流程检查当前网站，不要直接发布，先帮我检查页面、链接、图片、GitHub Pages 发布状态，并列出最后确认项。
```

### 2. 正式上线并发布 GitHub Pages

```txt
我已确认正式上线，请在 "/Users/hawksky/Desktop/modern phi/modern philosophy website" 执行最终发布，提交并推送到 GitHub Pages。
```

### 3. 正式上线并绑定自定义域名

```txt
我已购买域名，请帮我把当前网站正式上线到 GitHub Pages，并告诉我 CNAME 内容和域名 DNS 应该怎么配置。域名是：example.com
```

把 `example.com` 改成你实际购买的域名即可。

### 4. 正式上线后继续更新

```txt
请把当前网站作为正式线上版本处理，检查本次改动后直接发布到 GitHub Pages。
```

## 重要说明

- GitHub Pages 的服务器在 GitHub，不在你的本地电脑上。
- 你本地电脑只负责编辑、提交、推送。
- 你电脑关机后，网站仍然可以正常访问。
- 购买域名后，仍然可以继续使用 GitHub Pages 托管，不需要把网站迁移到你自己的本地服务器。
