# 思想殿堂连续阅读优化验收 · 2026-09-05

final result: passed

## 本轮结果

画中人物可直接打开资料；人物选择栏仍提供预览；资料工具条加入上一位、下一位与动态姓名/序号，支持七人循环阅读。小屏的不可见热点退出辅助技术和 Tab 入口，由可见的人名按钮承担键盘操作。新增“浏览中心内容 / Explore the center”锚点。首栏从“中心动态 / From the center”改为“活动速览 / Recent activities”，区分摘要与下方的完整活动、中心动态。

保留当前七人殿堂、肖像与真实来源、章节纸色、栏目分割线和出版物图片。本轮没有生成新图、部署或写入 Figma。

## 对照依据

输出目录：`/Users/hawksky/Desktop/modern phi/output/atheneum-reading-20260905/`。

- 桌面首页：`before-desktop.png` 与 `desktop-home.png`，均为 1448 × 1086、scrollY = 0、休谟选中、资料关闭；在同一次浏览器工具输入中并置查看。
- 桌面资料面板：`before-dialog.png` 与 `dialog-desktop.png`，均为 1448 × 1086、休谟资料打开、正文顶部；已在同一次输入中并置比较。新面板由 880 px 改为最多 960 px 宽，并采用稳定高度，连续切换时不随人物文字长度反复改变外框大小。
- 英文手机首页：前版 `../atheneum-refinement-20260905/mobile-en.png` 与本版 `mobile-home-en.png`，390 × 844，默认贝克莱、页面顶部；已并置比较。
- 阅读面板近距离检查：`mobile-reader-zh.png`（康德）与 `mobile-reader-en.png`（莱布尼茨），390 × 844；另实际查看 320 × 740 英文莱布尼茨，面板内容宽与 scrollWidth 相等，无横向溢出。
- 浏览器截图为 devicePixelRatio 1 的原始像素，不做图像重绘或密度归一化；滚动条出现时计入浏览器截图。人物面板本身已足够大，标题、来源链接与操作按钮清楚可读，无需额外裁图。
- 交互记录：`interaction-checks.json`。

## 发现与修复历史

| 优先级 | 发现 | 修改 | 复核 |
| --- | --- | --- | --- |
| P2 | 图上人物点击只更新下方卡片，需要再次操作才能阅读 | 热点直接打开资料，名称和 ARIA 描述明确为阅读动作 | 鼠标点休谟、键盘 Enter 开笛卡尔均通过 |
| P2 | 资料打开后不能连续切人 | 常驻工具条提供上一位/下一位及动态姓名和人数 | 正向遍历七人，反向从笛卡尔回休谟；资料、标题、序号同步 |
| P2 | ≤680 px 热点可聚焦，唯一焦点提示却被隐藏 | 小屏热点 tabindex=-1、aria-hidden=true；保留清楚可见的七个人名按钮 | 390 px 七个热点均退出入口；恢复桌面后七个热点恢复可用 |
| P2 | 从桌面热点打开资料后改成手机，关闭可能回到不可见焦点 | 返回原热点对应的人名选择按钮 | 休谟热点打开、缩至390后关闭，焦点回休谟人名按钮 |
| P2 | 新内容入口继承深色链接文字，深底文字不可见 | 提高入口样式优先级，前景设为浅金 #ffebbc | 中英文截图可读，实际样式为 rgb(255,235,188)，锚点跳转通过 |
| P2 | 英文 Next 按钮计算宽约43.8 px | 工具条按钮 min-width:44px | 320 px 手机四个操作/状态仍正常排列，无溢出 |

首次 End 键滚动检查在原生滚动尚未结束时取值，不能证明正文回顶；随后待界面稳定后复测：莱布尼茨 scrollTop=339，切到康德后 scrollTop=0，焦点仍在“下一位”。本报告采用后一次有效结果。

## 五项视觉验收

- **字体与排版**：沿用现有宋体与 Georgia，姓名、序号、正文和来源有清楚层级。窄屏长英文姓名正常换行。工具条不使用截断姓名来代替正文标题，顶栏短名从数据获取。
- **间距与构图**：首页只增加小型内容入口，殿堂人物位置未改变。资料面板稳定尺寸、正文留白一致；手机肖像和来源并排，正文顺序清楚；固定工具条在长内容滚动时可见。
- **色彩与状态**：保留旧纸与深棕、浅金色。入口在深底可辨；上一位、下一位、关闭按钮具有边框、hover、可见键盘焦点。
- **图像质量**：全部沿用七人殿堂与原始肖像，没有重新压缩、调色或替换面貌。新阅读布局保持图片比例，手机肖像限制高度时使用 contain。
- **内容**：全部人物、著作与来源保持既有核实数据。人数、姓名、索引由 JSON 驱动；上下人物 ARIA 名称同步。活动速览和详细活动的摘要/全文关系明确；未改动学术事实。

## 交互和技术检查

- 图上点击及 Enter 直接打开对应资料；选择器单击仍为预览。
- 正向七人循环、反向首尾切换，资料标题 aria-labelledby 与可见人物一致。
- 关闭/Escape 恢复原入口；手机从资料卡打开后 Escape 回到阅读按钮。
- 长资料滚动后切人回顶；切换按钮继续保持焦点。
- 小屏热点隐藏与恢复桌面热点均核实；在模态框中改变窗口尺寸不把焦点强制移出面板。
- “Explore the center”实际到 #atheneum-content，焦点落到对应区域。
- 中文桌面1448、手机390；英文手机390和320均查看。页面、资料面板及工具条无横向溢出。
- 浏览器 error/warn 日志为空。
- 12 项生成回归测试通过。现有 all_philosophers 测试新增直接阅读语义、短名转义、重排/减员后人数与位置检查。
- build_site.py --check：94 outputs，changed=[]。
- check_site.py：93 pages，4020 localReferences，errors=[]，warnings=[]。
- Node --check atheneum.js：通过。默认 shell 无 node，使用现有 bundled runtime `/Users/hawksky/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node`，未安装依赖。
- git diff --check：通过。

本轮无未解决 P0/P1/P2。可选后续内容维护是把英文首页较下方的静态活动摘要统一纳入数据生成，避免未来新增活动时维护两处；本轮实际展示未发生冲突。前版验收归档为 previous-design-qa.md。
